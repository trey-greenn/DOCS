```mermaid
flowchart TD
    U[User] --> F[Frontend - React + Vite + ShadCN UI]
    F -->|Login / Dashboard / Chat UI| B[Backend - FastAPI]
    B -->|Auth & API Routes| DB[(Shared Modules)]
    B -->|Stream Responses (SSE)| G[Gemini API]

    subgraph Frontend
      F
    end

    subgraph Backend
      B
      DB
    end

    subgraph External Services
      G
    end
